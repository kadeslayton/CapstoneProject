import { useState, useEffect } from "react";
import showCart from "../components/Cart.jsx"

export default function CartPage() {
  const [cartList, setCartList] = useState([]);

  return (
    <>
      <showCart key={index} id={id}/>
    </>
  ) 
}